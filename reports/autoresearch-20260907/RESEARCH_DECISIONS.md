# Current research priorities

The campaign seeks reusable findings and applications, not the largest development speed number. Each new direction needs a controlled comparison and an explicit reason to continue.

1. **Compact conditioning interface — primary new evidence.** Two late layers retain 97.63% [96.23, 99.03]% throughput with 60% fewer mapper parameters on 64 separately frozen requests. This establishes a practical compact interface in the tested DFlash setting. Mapper arithmetic is below 1% of request time, so fewer parameters should not be sold as a large latency gain.
2. **Native application — confirmed secondary evidence.** Native rank-1536 projection compression retains 98.66% [97.58, 99.75]% versus released native with 55% fewer projection parameters. Native two-layer distillation now passes a separate frozen64-question comparison:97.05%[95.93,98.21]% retention with60% fewer projection parameters; one layer retains84.55%. All five methods60/64correct with identical outputs and no capped answers. SVD and interface distillation are not claimed as new general compression algorithms.
3. **Calibration composition — replicated development insight.** The retargeted dialogue gain repeats at both fitting seeds on sixteen additional conversations: about 7.5%, compared with 0.3–1.1% for native interfaces. Similar proportional feature-error reductions have different decoding effects. Other initial workloads have intervals including parity. No dialogue-quality claim is available.
4. **Downward retargeting — applicability boundary passed in development.** The 4B-trained drafter works with the 0.6B verifier after 512-record fitting, giving 3.26× AR throughput on sixteen completed GSM8K answers and gains on code/dialogue screens. AR and speculative outputs can differ in BF16. This is an applicability example, not a fresh confirmation or recommendation about small-verifier quality.
5. **Numerical agreement — supporting evaluation rigor.** Full FP32 gives within-runtime AR agreement on all tested 0.6B math/code/dialogue cases and sixteen 8B questions. Numerical sensitivity and FP32 mitigation are established prior work (Yuan et al., 2506.09501). Head-only FP32 improves initial 8B agreement to 16/16 but only 9/16 for 0.6B and does not reproduce full-FP32 AR. Wave29 completed: head-only agreement improves15/32→20/32 GSM8K and3/16→5/16 MATH, equally for native and RelaySpec. Native/relay sequences match in every arm. Close this as a partial mitigation, not a general repair. Do not let this supporting diagnostic displace the interface contribution or become an indefinite precision sweep.
6. **Early-layer nonlinearity — closed negative direction.** Parameter-matched MLP training fits features better but does not close the large early/late decoding gap. No additional width sweep is justified by the completed eight-question screen.
7. **Compression objective — retain the family-dependent result.** EAGLE benefits much more from fitting activation outputs than preserving weights. Direct fitting remains competitive. Cite ASVD/SVD-LLM and keep unequal total adaptation budgets explicit.

Selective capture is now implemented and passed waves30–31 with exact paired trajectories. At31526 synthetic input tokens the two-tap mapper reduces peak allocation32.73→25.03GiB, with essentially unchanged runtime. Wave32 passed: identical savings after prompt prefill-buffer release, all trajectory controls pass. Wave33 now checks code/dialogue correctness. Treat this as an implementation consequence of the compact interface, not a novel general memory algorithm or task-quality evidence.

Avoid fine rounding sweeps, more interpolation coefficients, or selecting the best exposed question subset. Fake-quantization runs execute BF16 and cannot establish integer-kernel efficiency. Every research lane retains a 540-second cap, and at most four GPUs are allocated across the dependency chain. Keep failed and negative arms in the registry. Full-PDF visual review should follow a stable manuscript build, not be falsely carried forward across changing PDFs.

Smaller-verifier compactness boundary (waves35–36) is closed after a targeted recovery check. Late two-layer maps retain82.2%, versus52.5% early, on eight exposed0.6B requests. Parameter-matched MLPs improve feature losses but retain81.1–83.1% across two seeds; factorized83.1–84.2%, paired late dense82.8–84.3%. Do not extend width/epoch sweeps without a new hypothesis. Preserve this limit alongside the successful8B retargeted/native confirmations. Next priority is manuscript evidence/positioning audit, not another minor fitting sweep.

## Completed 14B learning-rate follow-up

Waves 37 and 38 close the four previously deferred EAGLE-3/14B rate checks. All fixed 8192-update fits pass within 8m38s and all matched 512-token decoding lanes within 4m15s. Lower rates reduce every validation endpoint and avoid the saved wide-linear spike. Dense rates 0.0002 and 0.00006 gain 6.94% and 3.72% versus their original matched map; MLP gains 4.43%, linear 1.69% with an interval crossing equality. Eight exposed MATH questions and four capped outputs per method make this a development finding, not quality confirmation. Keep both dense rates: lower feature error does not select the faster map. This closes the targeted optimization confound, not a justification for an unbounded rate sweep.

## Native code execution follow-up

Wave39/job28529 completes in2m07s. On32 additional-to-autoresearch but historically exposed MBPP tasks, all five native variants produce identical token sequences and extracted code, pass28/32 published base tests, and have zero1024-token cap outputs. Two layers retain95.43%[93.54,97.30]% throughput, one layer71.56%, SVD97.68%. The point estimate supports applicability but the interval would not establish a95% retention floor on code. No such threshold was declared for this functional development check. Keep all four shared test failures and the distinction between base tests and EvalPlus extended tests. No optional extension of this32-task protocol.

## Compact interface calibration sensitivity

Waves40–41 pass in2m52s and2m51s. Same target-layer pair, cached record prefixes,8192 batch-four updates, seeds1729/1730, and8 shared exposed GSM8K questions. Relative to each role's original512-record two-layer map, native128 retains97.16–97.91%, retargeted12887.66–88.94%; native1666.66–66.99%, retargeted1646.30–47.12%. The16-record fits have tiny training error but much higher held-out feature error. Role-specific teachers, output widths and normalization are retained, so this does not identify intrinsic task sample complexity. Seeds share questions and reference maps. Next mechanistic hypothesis, if pursued: at very few records, initialization/unsupported input directions may matter for extrapolation; do not extend record counts merely to refine a threshold.

## Native initialization intervention

Wave42/job28532 passes in2m55s. At16 records, selected released-column initialization reduces native two-layer validation error0.23323→0.05591 and raises throughput versus the original512-record compact map0.6699→1.0071. At128 records the corresponding errors are0.04278→0.03399 and ratios0.9669→1.0059. Same teacher, record prefixes, seed1729,8192 updates and optimizer settings; initialization records and teacher hashes audited. This is an eight-question development intervention. Do not claim16 records suffice universally or that calibration itself explains the result until the untrained cropped-native control is measured. Wave43/job28533 pairs that control and all four fits within each GPU across GSM8K, MATH, code and dialogue, with540-second lane limits.

Wave43/job28533 passes all four lanes in5m47s. Full repacked-native-relative retention for untrained cropping is95.67/90.84/90.65/98.38% on GSM8K/MATH/code/dialogue. Sixteen-record inherited initialization gives99.79/93.53/92.64/98.39%, while random16 gives66.58/68.97/52.65/72.73%. At128 inherited gives100.63/96.21/94.19/98.10%. Thus inheritance rescues severe sparse-data failure, but much performance already exists in untrained selected columns. Calibration improves math screens, code intervals cross cropping parity, and dialogue scarcely changes. Preserve that control and do not pitch all benefits as newly learned. Wave44/job28535 tests128/1024 updates at16/128 records to bound calibration work. Job28534 was cancelled with zero runtime because its source snapshot lacked the uncommitted config; corrected committed snapshot214386d launched28535 with the same after28533 dependency. No overlap beyond four GPUs.

Wave44/job28535 passes in1m28s. With inherited columns,128 updates retain99.31% at16 records and101.03% at128 records relative to the512-record compact reference;1024 updates retain98.61/100.74%. Validation errors are lower at128 than1024 or8192 updates in these cells. This suggests early calibration is sufficient in the exposed GSM8K screen, but raises an optimization confound for the original initialization contrast. Wave45/job28536 therefore runs random16/128 at128 updates and inherited16/128 at128 updates with seed1730. Close that control before adding the initialization finding to the main manuscript.

Wave45/job28536 passes in1m25s. Matched128-update random maps retain51.68% at16 records and72.45% at128, versus inherited99.31/101.03% from wave44. Inherited seed1730 runs retain98.71/100.68%, but their fitting losses and checkpoint tensor-storage SHA256 are exactly identical to seed1729. The fixed record order and overwritten random initialization make these repeat executions, not independent fitting-seed replication. This is explicitly recorded in native-short-repeat-storage.json. Wave46/job28537 compares short-fit random/inherited candidates with cropped, full and512-record compact references across four domains. Keep the eight-question scope and caps. The initialization-budget graph connects separately fitted endpoint measurements, not checkpoints from a continuous run, and omits the deterministic repeats.

Wave46/job28537 passes in6m03s. At128 updates, inherited16-record maps retain98.61/94.54/94.41/97.69% of full repacked native across GSM8K/MATH/code/dialogue. Matched random16 retains51.59/52.00/36.80/63.14%. Untrained crops retain95.90/90.76/90.79/98.42%. All paired output hashes match, but MATH/dialogue caps remain. The paper now includes the initialization paragraph, both budget campaigns in Table25, and a main-text pointer. Keep the distinction between inherited performance and calibration gains. Automated audit passes all checks except the matching visual-review record, which is still pending for the updated47-page PDF.

Wave47/job28538 passes in1m18s. Four disjoint16-record training windows at offsets16/32/48/64 retain99.99/99.10/100.05/99.11% versus the512-record compact reference, with validation errors0.0368–0.0377. Training selection manifests match the frozen cache prefix and record hashes are disjoint from each other and the initial16. This is actual calibration-record variation, unlike the deterministic nominal-seed repeats. Added this bounded robustness result to the paper paragraph. Current visual review must follow the final rebuilt PDF. No new holdout questions were consumed.

The47-page initialization/subset manuscript now passes full visual and automated audit (native-init-final-audit). Main technical text remains nine pages. Next direction is two-scalar native block calibration: freeze both inherited projection blocks, learn one gain per layer and fold the gains into the standard dense export. This tests whether relative block contributions explain the calibration benefit without adapting33.55M projection directions. Wave48/job28539 runs16/128 records at128 updates and scalar learning rates0.003/0.03. The two rates are a development screen, not a matched-rate comparison with full-map Adam. Tests verify exactly two trainable parameters, no frozen-weight gradient, and folded-export equivalence. BF16 batch-gradient equivalence remains required inside every actual GPU fit. All lanes540seconds, max4 GPUs.

Wave48/job28539 passes in1m18s, all actual BF16 batch-gradient pilots pass and exactly two parameters train. Gains converge to similar relative weighting of layers25/33, around1.11–1.12. Validation error remains0.07327–0.07332 versus0.0377 for full inherited16-record fitting. Throughput versus512-record compact reference ranges95.84–97.36% across the four scalar settings. This does not yet separate scalar benefit from untrained cropping or runtime variation. Wave49/job28540 therefore pairs all four settings, full inherited16/128 fits, untrained crop, full native and512-record compact reference on all four development workloads. Nine methods per worker, every lane540seconds. Do not select only the best scalar arm or call scalar fitting a full replacement before those controls finish.

## Native calibration controls and confirmation, waves48–59

The independently frozen 16-record/128-update native candidate **fails** its predeclared 95% retention criterion on64 new GSM8K questions:95.09%[94.03,96.13]%. The128-record control gives96.60%[95.48,97.70]% but is secondary and cannot replace the failed primary. All five methods produce identical outputs,62/64 correct, zero cap hits. Conservative paired accuracy interval remains±6.62pp. Reserve consumption is now192/256, leaving64 unused. These results and the failure are in the main-paper pointer and Appendix C, Table26. The48-page PDF, nine-page main text, passed the manuscript and visual audit after this integration.

The two-scalar layer-reweighting control does not reliably improve untrained cropping on any of the16 task/settings comparisons. Full fitting beats the scalar arms on the MATH screen. Close the tested scalar direction rather than search many rates to force a gain. The single-layer inherited screen improves on random initialization but its best short endpoint retains only85.6% of the original512-record two-layer map. This does not justify a new near-native80%-compression claim.

A low-rank correction to frozen inherited columns is more promising. Rank128 trains1,572,864 parameters instead of33,554,432 full-matrix parameters, then folds into the same deployed dense projection. At128 records/128 updates/lr0.006, it retains99.6–100.5% of full128-record fitting throughput across the four exposed workloads, with all paired intervals crossing parity. This is a standard low-rank adaptation control, not a new low-rank algorithm or a deployed-size saving. All four initial rank/rate arms remain recorded. Additional seeds1730/1731 favor rank128 in validation error but do not consistently favor it on the eight-question GSM8K throughput screen. Wave58 tests all four seed/rank controls across workloads before any stable-rank claim or paper integration.

Native EAGLE3 export is now verified against the released native implementation on four short workload pilots: full-projection outputs and acceptance trajectories match exactly. The first attempt failed before decoding due to absent proposer-family metadata, was retained as wave54, and the corrected wave56 passed. Native EAGLE calibration preserves the raw post-fusion convention without an external RMS norm. Matched128-update inherited versus random fits retain93.2% versus50.6% at16 records and93.4% versus62.4% at128 records on eight exposed GSM8K requests. Wave59 compares all four fits against untrained cropping and rank1536 SVD across workloads at a256-token cap. The initialization benefit extends to another family in development; near-full DFlash retention must not be generalized to EAGLE without the controls.

Next decisions: finish waves58/59, keep all outcomes, and integrate only the supported mechanism and family boundary. Preserve the last64 reserve questions. The goal remains active. Four GPUs maximum, every lane capped540seconds. The live jobs ledger is authoritative for current execution.

## Native EAGLE confirmation and sampled decoding, waves60–74

This entry supersedes the earlier reserve accounting: all256 reserve questions are now consumed across four separately frozen64-question studies. Wave65/job28560 passes the native EAGLE primary criterion: inherited512-record8192-update late-two-layer map retains98.78%[97.67,99.96]% released-native throughput with60% fewer projection weights. All five arms score59/64, identical outputs, zero2048-cap hits. Short128-update control retains97.42%, data-free weight-SVD1536 retains92.44%. Calibration-data costs differ. No tight quality noninferiority or general compression superiority claim. Native EAGLE development also shows longer fitting helps GSM/MATH but hurts code, limiting universal feature-loss claims. The manuscript now includes the two-family native result in its abstract, main section and conclusion, with all frozen controls and workload budgets in the appendix.

Rank128 native residual correction now has three fitting seeds across four tasks, retaining97.3–100.5% of full128-record fitting in point estimates while training95.3% fewer parameters. It is standard low-rank adaptation folded into the same deployed projection, not a new algorithm or an additional deployed-size saving. The native-correction paragraph retains scope, rates, seeds and losses.

Nonzero-temperature operational wave60 lacked explicit Torch sampling seeding and is excluded from reproducible sampling claims. Wave62 reruns with per-request derived seeds and passes strict byte-identical checkpoint trajectory controls in both families at temperatures0.6/1.0. Wave64 is a separate8-question full-answer study with zero cap hits. Frozen sampling-breadth-v1 subsequently uses32 different previously exposed questions and two different seed bases1730/1731, both families and temperatures, frozen512-record retarget maps and AR/native controls. Its768 outputs are analyzed only after all eight waves66–73 complete, clustering both seeds together by question. No32+8 pooling, quality noninferiority or distributional exactness claim.

Wave74/job28569 is a bounded code-mechanism probe: native EAGLE layer sets[25,33],[17,25,33],[9,25,33],[1,25,33], matched512 math records128 updates and inherited columns. Each worker pairs its new fit with full native and the frozen two-layer short fit. Eight exposed code requests256cap. This tests whether an additional earlier feature helps the code deficit, without a general best-layer or functional-quality claim. Source2ebf9c0, every lane540seconds, queued after all sampling waves, max4 GPUs.

The collector had stopped at cancelled-before-start job28534, which has no output directory. It now records zero-duration cancellations explicitly without expecting artifacts, and the service was relaunched. All terminal sampling results are being collected. No experiment was silently retried.

Sampling-breadth-v1 completed all768 outputs, all32 lanes passed, every four-GPU wave took2m00s–2m33s. Zero cap hits. Relay/AR throughput atT0.6/T1.0: DFlash3.721/3.196, EAGLE3 2.639/2.474. Correct counts outof64 sampled answers: DFlash59/61, EAGLE61/60, AR60/59 in both families. Native retention87.67/80.21% for DFlash and97.67/95.45% for EAGLE. Bootstrap questions jointly across both seeds. Sparse discordances and conditional two-seed intervals cannot establish quality NI or distributional exactness. The12-row table and explicit scope are in Appendix E.1 with a main conclusion pointer.

Wave74 passes2m06s. Refit two-layer control matches the existing short fit closely. Adding layer17 gives3.05%[0.95,5.27]% throughput gain relative to paired two-layer, retains98.03% of full native and lowers validation error0.03364→0.00983. Adding9 gives1.51%[-0.98,3.81]%, adding1 gives1.94%[0.82,3.08]%. All8 outputs match,3 cap hits. Do not rank additions confidently from8 exposed requests. Wave75/job28570 freezes all three additions with two-layer, repacked-full and released-native references on32 MBPP questions disjoint from those8, with1024cap and isolated base-test scoring. Six arms perworker, four8-question shards, all540seconds. Source1ad5540. No independent fresh-confirmation claim or selected primary.

## Native code capacity and calibration, waves75–78

Wave75/job28570 completes4m11s, all four lanes pass. On32 MBPP development tasks disjoint from the preceding8-question screen, native EAGLE [17,25,33] retains98.67%[97.35,99.90]% of released native with40% fewer projection weights. Two layers retain92.94%[91.46,94.46]%, [9,25,33]96.61%, [1,25,33]94.58%. Every method passes25/32 base-test suites and avoids the1024cap. Three-layer maps match32/32 native token sequences, two-layer31/32. All six arms are retained, not a selected-primary confirmation. Correctness intervals remain wide.

Wave76/job28571 completes5m02s, all four lanes pass. First16 fixed code-breadth tasks compare each fitted interface with its untrained native-column crop, full repacked and released native. All arms pass12/16 base-test suites, no caps. Crop/native retention is81.30% (two),94.66% (+17),92.31% (+9),86.88% (+1). Fitted/crop ratios1.138[1.112,1.165],1.026[1.002,1.050],1.039[1.017,1.060],1.078[1.046,1.108]. These are paired development intervals without multiplicity correction, on the same16 questions, not64 independent tests. Calibration adds value even though inherited columns explain much of the performance. Main capacity prose and appendix tables now retain this workload-specific capacity result and its calibration control. New51-page build is awaiting final visual/automated audit after the14B coverage update.

Waves77/78, jobs28572/28573, freeze the original14B dense512/dense2048 and matched factorized4096/MLP4096 at2048, all8192updates lr0.0006, plusAR.32 previously exposed GSM8K questions shared with the sampled8B cohort, four8-question shards per family. Max2048 outputs,320 declared outputs. This closes a limited full-answer14B capacity gap without claiming optimized-recipe dominance or fresh confirmation. Source snapshot is recorded in jobs.json. No new training. Every lane540seconds, max4 GPUs, dependency serialization.


## Completed14B answers, instruction styles, and sampled native controls

Waves77/78 complete4m08s/4m56s, all320 outputs present, all ten cells31/32 correct, zero2048-token cap hits. Dense512 retains97.53%[95.77,99.48] and97.17%[95.36,99.25] of dense2048 in DFlash/EAGLE. Matched factorized4096 retains98.80%/100.28%, MLP4096 92.14%/95.73%. This is an original-rate development comparison, not optimizer-optimal ranking. Main and appendix updated.

Wave79/job28574 completes3m42s. Eight paired GSM8K questions with terse/explained user instructions, two families, AR/native/relay, all192 answers correct with no caps. Concision lowers output length and AR-relative speed. All native-retention interaction intervals include one. Close as diagnostic, not a paper headline.

Wave80/job28575 completes1m57s, all128 answers correct without caps. Native EAGLE two-layer retains101.5%/94.9% at temperatures0.6/1.0, versus SVD91.8%/93.3%. DFlash full repacked control itself retains92.4%/78.9%, making compression-only attribution invalid. Source inspection finds released DFlash uses greedy proposals regardless of target temperature, while RelaySpec sampled proposals at target temperature. Wave81/job28576 aligns both DFlash relay/source-reuse proposal policies to released DFlash, leaves target sampling and all greedy runs unchanged, freezes same eight questions and seed1732. Two native compression and two retargeting lanes, all540seconds. Old sampling measurements remain historical evidence, now explicitly qualified in the paper. The sampling-policy fix is not a new algorithm.


Wave81/job28576 completes1m56s, all four lanes pass, all112 answers correct with no caps. With released-style greedy proposals, full native DFlash repacking matches all8 output hashes, acceptance sequences, target and draft call counts at both temperatures. Retention is99.12%/99.26%, versus historical92.4%/78.9%. Native two-layer retains96.53%/94.02%, SVD99.40%/103.04%, conditional eight-question intervals are wide. Retargeted dense512 retains87.23%/88.22% of native. Waves82–85 freeze a matched-policy repeat of the prior32 exposed questions and both sampling seeds, DFlash only,384 answers, four serial four-GPU waves. EAGLE sampling behavior is unchanged and does not require this runtime correction.

The52-page PDF, main9, passes the complete manuscript audit after explicit proposal-policy qualification and final visual review. All26 evidence builders reproduce30 autoresearch tables/plots,47 citations resolve, fonts embedded and no overflow/undefined references. This is an evidence/presentation check, not a scientific acceptance guarantee.


## Matched-policy sampling breadth and activation-aware native controls

Waves82–85/jobs28577–28580 complete2m02s,2m11s,2m08s,2m18s. All16 lanes pass and384 outputs score with zero cap hits. DFlash at temperatures0.6/1.0 reaches148.23/141.29tokens/s,3.9049[3.6499,4.1693]/3.7924[3.5586,4.0475] times matched AR. Native retention92.5523%/94.4159%. Correct60/64 and61/64 sampled answers, versusAR60/64 and59/64, native61/64 and62/64.32 question clusters, two seeds, no quality NI. Current paper table uses these384 DFlash outputs and the unchanged384 EAGLE outputs, with the historical draft-sampled DFlash measurements explicitly distinguished. All historical outputs remain in artifacts.

Wave86/job28581 tests native activation-aware rank1536 against matched weight-only rank1536, full native/repacked and original512-record two-layer maps, both drafter families on8 GSM8K and8 MBPP tasks. Activation fitting reuses512 math-cache records with at most8 positions each and128 fitting-validation records. Cache target revision and raw-tap convention were inspected. This is established output-subspace compression, not a novel algorithm. Total adaptation budgets differ from weight-only and two-layer fitting. Four lanes, all540seconds.


Wave86 passes all four lanes in3m19s,160 outputs scored, no caps. All outputs match native within cell, math8/8 and code8/8 DFlash or7/8 EAGLE across all arms. Native-relative activation/weight/two-layer retention: DFlash GSM101.11/97.92/96.56%, code99.45/99.66/97.83%; EAGLE GSM95.13/97.64/98.33%, code88.42/92.25/94.61%. Small conditional screen, no superiority claim. Calibration projection MSE is0.000235 DFlash and0.002161 EAGLE. Identical transform hashes reproduce in both workload workers per family. This motivates retaining a stronger compression baseline and testing its family/workload boundary. Waves87–90 freeze all five methods on the same32 exposed math and32 exposed code tasks, both families,640 outputs. Original8 are part of these cohorts, not extra independent questions. No new fits or selected-primary confirmation.

Matched-policy sampling update passes the complete52-page manuscript audit and exact-PDF visual review, with main9 preserved. Historical DFlash draft-sampled results stay in artifacts and are described separately.


## Code-domain calibration follow-up

CodeAlpaca source pinned at2f78ddc5c682ed6738ad092bbbfa59ba915afcb0, original20,022 rows. A deterministic Python-keyword instruction subset yields2,584 candidates. First640 retained after exact and5-shingle Jaccard0.6 exclusions against10,354 unique repository-config texts and earlier selected records:128 validation,512 training. Three exact and six near matches excluded before reaching640. Synthetic code correctness is not asserted. Source data licenseCC BY-NC4.0 and attribution retained. This filter does not prove semantic/pretraining independence.

Job28586/wave91 is the existing EAGLE cache/gradient-equivalence/duplicate-map pilot on64training16validation code records. Job28587/wave92 is conditional on successful pilot and extracts512training128validation records. Both request4 GPUs with540-second timeout, serialized after the compression breadth jobs. Their purpose is to enable matched math-versus-code calibration for native EAGLE two-/three-layer interfaces, testing domain mismatch versus a capacity requirement. No new source or drafter transformer training. Pipeline gates are explicitly registered in the experiment ledger.


## Native compression breadth and code-calibration pilot completed

Waves87–90/jobs28582–28585 pass all16 lanes in3m12s,3m20s,2m51s,3m35s. All640 outputs scored. DFlash activation/weight/two-layer native retention99.52/98.32/97.22% on32 GSM8K and97.99/98.57/95.97% on32 MBPP. EAGLE94.10/95.26/96.54% on math and86.13/90.01/92.98% on code. All methods have identical within-cell base-score counts. EAGLE code has one cap per method and31/32 token matches for weight/activation,32/32 for two-layer. Retain all methods and limits. New20-row table in appendix.53-page PDF with main9 passes full visual and manuscript audit,31 autoresearch assets from27 builders.

Code pipeline28586/28587/28590 passes2m17s/34s/2m08s, full cache512train128validation. Wave94/job28604 passes2m26s, four native EAGLE fits crossing math/code calibration with2/3 retained layers,512records128updates. All8 exposed MBPP outputs match native,7/8 base-test passes, no caps. Native retention math2=94.64%, math3=99.72%, code2=94.59%, code3=101.13%. Separate-worker pilot, not a domain-effect conclusion. Wave95/job28605 freezes all four fits in a six-method within-worker32-task comparison, four8-task shards,1024cap,540seconds/lane. Pilot8 are a subset, not additional confirmation. No method selected away.
